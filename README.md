# install react-toastify
    use to show the notification

# dependencies":
    "bcrypt": "^6.0.0",       // Used to hash (encrypt) passwords before storing them in the database and compare hashed passwords during login.

    "cloudinary": "^2.10.0",  // Used to upload, store, optimize, and manage images and videos on Cloudinary cloud storage.

    "cors": "^2.8.5",         // Enables Cross-Origin Resource Sharing so the frontend and backend can communicate from different origins.

    "dotenv": "^17.4.2",      // Loads environment variables from a .env file into process.env.

    "express": "^5.2.1",      // Fast and lightweight Node.js framework for building web servers and REST APIs.

    "jsonwebtoken": "^9.0.3", // Used to create, sign, verify, and decode JWT tokens for user authentication and authorization.

    "mongoose": "^9.7.2",     // MongoDB ODM (Object Data Modeling) library for defining schemas and interacting with MongoDB.

    "multer": "^2.2.0",       // Middleware for handling multipart/form-data, mainly used for file uploads.

    "nodemon": "^3.1.14",     // Automatically restarts the Node.js server whenever file changes are detected during development.

    "razorpay": "^2.9.6",     // Official Razorpay SDK for integrating Razorpay payment gateway.

    "stripe": "^22.3.0",      // Official Stripe SDK for integrating Stripe payment gateway.

    "validator": "^13.15.35"  // Library for validating and sanitizing strings such as email, URL, phone number, etc.
  